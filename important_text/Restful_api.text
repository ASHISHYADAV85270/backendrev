restfull api =>
Rule
1-> Server Client Architecture => both are independent of eachother 
    (a)=>if u know that Client is browser then always use html as a return and it is known as Server Side Rendering 
    (b)=>if u have cross flatform then always send json it is known as Client side Rendering done in reactjs

2-> always respect all http method => GET POST PUT DELETE PATCH
    GET/user=> user data read and return
    POST/user=> user data create 
    PATCH/user=> user data update

